type Query {
  user_tweets: [Tweet!]!
  get_states: Territories!
}

type Tweet {
  text: String
  user: User!
}

type User {
  name: String!
  profile_image_url: String!
}

type Territories {
  count: Int!
  states: [State!]!
}

type State {
  name: String!
  abbreviation: String!
}
